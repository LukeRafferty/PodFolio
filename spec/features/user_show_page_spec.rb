require 'rails_helper'

feature "user has a show page" do
  scenario "while logged in, user can see their podcasts" do

  end
  scenario "user cannot navigate to their user page until logged in" do
    
  end
end
